parameters:
    level: 1
    paths:
        - src/
        - tests/
    ignoreErrors:
        # Drupal's standard ContainerFactory DI pattern; safe in plugin context.
        - '#Unsafe usage of new static\(\)#'
